{
  "name": "ascendria-site",
  "version": "1.0.0",
  "description": "Ascendria - Gaming Community & NFT Ecosystem Landing Page",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "build": "node build.js",
    "build:prod": "node build.js",
    "preview": "npx serve dist",
    "lint": "eslint . --ext .js --ignore-path .gitignore",
    "lint:fix": "eslint . --ext .js --fix",
    "clean": "rm -rf dist",
    "validate": "npm run lint && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.27.1",
    "three": "^0.160.0",
    "eslint": "^8.57.0",
    "sharp": "^0.34.5"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
